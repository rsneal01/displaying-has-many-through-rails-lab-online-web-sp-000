<% @patient.appointments.each do |appointment| %>
    <% a = appointment.appointment_datetime.to_formatted_s(:long).split(" ") %>
    <%= a[0] + " " + a[1] + " " + a[2] + " at " + a[3] %>
    <br>
    <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %> said
<% end %>